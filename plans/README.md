# Implementation Plans

Generated by the `improve` skill on 2026-08-16. Execute in order unless the dependency column says otherwise. The workspace was not a Git repository when these plans were written, so plan 001 creates the baseline used by every later drift check.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Establish a safe Git baseline | P1 | S | — | DONE |
| 002 | Establish one-command verification | P1 | M | 001 | DONE |
| 003 | Bind the local server to loopback | P1 | S | 001, 002 | DONE |
| 004 | Make shadcn MCP execution reproducible | P2 | S | 001, 002 | DONE |
| 005 | Replace generated onboarding documentation | P2 | S | 001–004 | DONE |
| 006 | Establish domain, application, infrastructure, and worker boundaries | P1 | L | 001–003 | DONE |
| 007 | Decompose the UI spike along server/client and feature boundaries | P2 | M | 002, 006 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED (with rationale)

## Baseline and drift convention

Plan 001 must create the repository's root commit. In plans 002–007, `<BASELINE_SHA>` means the output of `git rev-list --max-parents=0 HEAD`. Before executing one of those plans, substitute that value in its drift command. Stop if there is more than one root commit or plan 001 is not DONE.

## Dependency notes

- 002 follows 001 so tests and future changes have a reviewable baseline.
- 003 and 004 use the verification command created by 002.
- 005 must document the commands as they exist after 002–004.
- 006 establishes architectural boundaries before more product behavior is added.
- 007 follows 006 so UI modules depend on stable application contracts rather than fixtures becoming accidental domain models.

## Findings considered and rejected

- SQLite single-writer behavior: accepted explicitly by ADR 0001 for one user and one worker.
- No application authentication: intentional for a loopback-only local application; revisit only if remote or multi-user access is introduced.
- Placeholder links and mock actions: deliberate UI-spike behavior; plan 007 isolates them but does not invent product behavior.
- Missing full discovery/browser/runtime pipeline: planned MVP work, not a defect in the current UI scaffold.
- Dependency vulnerabilities: `pnpm audit --audit-level high` reported none on 2026-08-16.
