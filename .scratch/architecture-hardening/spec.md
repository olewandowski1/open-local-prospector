# Architecture Hardening

Improve maintainability without changing product behavior. Concentrate on enforceable feature
boundaries and the largest mixed-responsibility infrastructure module before considering broader
structural changes.

The work must preserve the local-first SQLite architecture, public feature interfaces, backup
format, workspace safety checks, and existing user-visible behavior.

